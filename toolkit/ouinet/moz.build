# -*- Mode: python; indent-tabs-mode: nil; tab-width: 40 -*-
# vim: set filetype=python:

FINAL_TARGET_FILES["Ouinet"] += [
    "download_ouinet.py",
    "metrics-server-cacert.pem",
    "ssl-inj-cert.pem",
]

if CONFIG["OS_ARCH"] == "WINNT":
    DIRS += [ "ceno-network-client" ]
